#include <stdio.h>
#include <pthread.h>
#include <semaphore.h>

sem_t	semaphore;

#define THREAD_NUM 10

int	main(void)
{
	pthread_t	th[THREAD_NUM];
	int			i;

	i = 0;
	sem_open()
	while(i < THREAD_NUM)
	{
		
	}
}
